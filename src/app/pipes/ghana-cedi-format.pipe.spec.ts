import { GhanaCediFormatPipe } from './ghana-cedi-format.pipe';

describe('GhanaCediFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new GhanaCediFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
